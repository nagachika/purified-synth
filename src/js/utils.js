export const CELL_WIDTH = 10; // px

// Left header column of every sequencer row (track controls, marker/ruler
// spacers, scroll-row spacer). Width is border-box; keep in sync with
// TrackControls#style_host in src/track_controls.rb.
export const TRACK_HEADER_WIDTH = 190; // px
export const TRACK_HEADER_GAP = 10; // px (margin-right)
